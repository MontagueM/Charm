<Window x:Class="Charm.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Charm"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        xmlns:views="clr-namespace:Charm.Views"
        mc:Ignorable="d"
        Title="MainWindow" Height="720" Width="1080"
        TextElement.Foreground="{DynamicResource MaterialDesignBody}"
        Background="{DynamicResource MaterialDesignPaper}"
        TextElement.FontWeight="Medium"
        TextElement.FontSize="14"
        FontFamily="{materialDesign:MaterialDesignFont}">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <!-- <RowDefinition Height="3*" /> -->
            <RowDefinition Height="100*" />
        </Grid.RowDefinitions>
        <views:CharmTabControl Grid.Row="0" x:Name="MainTabControl" materialDesign:RippleAssist.IsDisabled="True">
            <TabItem Header="LISTS">
                <views:ManyListsView/>
            </TabItem>
            <TabItem Header="MAIN MENU" x:Name="MainMenuTab">
                <!-- <views:MainMenuView/> -->
            </TabItem>
            <TabItem Header="ACTIVITIES">
            </TabItem>

            <TabItem Header="" x:Name="Spacer">
            </TabItem>
            <TabItem Header="CONFIG">
                <!-- <views:ConfigView/> -->
            </TabItem>
            <TabItem Header="LOG">
                <!-- <misc:LogView/> -->
            </TabItem>
            <TabItem Header="DEV">
                <!-- <views:ConfigView/> -->
            </TabItem>
        </views:CharmTabControl>
        <!-- <views:ProgressView x:Name="ProgressView" Grid.Row="0"/> -->
    </Grid>
</Window>
