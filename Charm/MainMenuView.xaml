<UserControl x:Class="Charm.MainMenuView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Charm"
             Loaded="OnControlLoaded"
             mc:Ignorable="d"
             d:DesignHeight="1080" d:DesignWidth="1920">
    <Grid Background="#1d1d1d">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100*"/>
            <ColumnDefinition Width="200*"/>
            <ColumnDefinition Width="100*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="100*" />
            <RowDefinition Height="30*" />
            <RowDefinition Height="100*" />
        </Grid.RowDefinitions>
        <!-- AB8FA380 variks -->
        <!-- 3b00ab80 atraks -->
        <!-- 4901ab80 taniks normal, 100bb780 abomination -->
        <TextBox Grid.Row="1" Grid.Column="1" TextAlignment="Center" FontSize="96" Text="6E35D580" KeyDown="TagHashBoxKeydown" x:Name="TagHashBox"/>
    </Grid>
</UserControl>
