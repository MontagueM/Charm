<UserControl x:Class="Charm.EntityListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Charm"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100*" />
            <ColumnDefinition Width="100*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="5*"/>
            <RowDefinition Height="100*"/>
        </Grid.RowDefinitions>
        <local:EntityView Grid.Column="1" Grid.RowSpan="2" x:Name="EntityView"/>
        <ScrollViewer Grid.Column="0" Grid.Row="1" x:Name="ScrollView">
            <StackPanel x:Name="EntityPanel" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Focusable="false">
            </StackPanel>
        </ScrollViewer>
        <StackPanel Orientation="Horizontal">
            <CheckBox IsChecked="True" Checked="ToggleButton_OnChecked" Unchecked="ToggleButton_OnUnchecked" x:Name="TrimNamesCBox">Show trimmed names</CheckBox>
        </StackPanel>
    </Grid>
</UserControl>
