<UserControl x:Class="Charm.ActivityView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Charm"
             mc:Ignorable="d"
             d:DesignHeight="1000" d:DesignWidth="1900">
    <Grid>
        <TabControl x:Name="ActivityTabControl">
            <TabItem Header="MAP" x:Name="MapTab">
                <local:ActivityMapView x:Name="MapControl"/>
            </TabItem>
            <TabItem Header="DIALOGUE" x:Name="DialogueTab">
                <local:ActivityDialogueView x:Name="DialogueControl"/>
            </TabItem>
            <TabItem Header="DIRECTIVE" x:Name="DirectiveTab">
                <local:ActivityDirectiveView x:Name="DirectiveControl"/>
            </TabItem>
            <TabItem Header="MUSIC" x:Name="MusicTab">
                <local:ActivityMusicView x:Name="MusicControl"/>
            </TabItem>
        </TabControl>
    </Grid>
</UserControl>
