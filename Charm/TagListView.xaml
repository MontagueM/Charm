<UserControl x:Class="Charm.TagListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:Charm"
             mc:Ignorable="d"
             Loaded="OnControlLoaded"
             d:DesignHeight="1080" d:DesignWidth="1920">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="100*" />
            <ColumnDefinition Width="100*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="5*" />
            <RowDefinition Height="100*" />
            <RowDefinition Height="5*" />
        </Grid.RowDefinitions>

        <TextBox x:Name="SearchBox" TextChanged="SearchBox_TextChanged" HorizontalAlignment="Left" Margin="102,0,0,0"
                 TextWrapping="Wrap" VerticalAlignment="Center" Width="700" FontSize="30"
                 FontFamily="{DynamicResource ResourceKey=Neue Haas Grotesk Text Pro 65 Medium}" FontWeight="36" />

        <StackPanel Orientation="Vertical" HorizontalAlignment="Right" Margin="0,2,30,0">
            <CheckBox Checked="TrimCheckbox_OnChecked" Unchecked="TrimCheckbox_OnUnchecked" x:Name="TrimCheckbox"
                      IsChecked="True" VerticalAlignment="Center" VerticalContentAlignment="Center"
                      HorizontalAlignment="Left" Content="Trim names" />
            <CheckBox Checked="ShowNamedCheckbox_OnChecked" Unchecked="ShowNamedCheckbox_OnUnchecked" x:Name="ShowNamedCheckbox"
                      IsChecked="True" VerticalAlignment="Center" VerticalContentAlignment="Center"
                      HorizontalAlignment="Left" Content="Filter named" />
        </StackPanel>


        <ListView x:Name="TagList" Grid.Row="1" Grid.RowSpan="2">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <ToggleButton Height="50" HorizontalContentAlignment="Stretch" Tag="{Binding Hash}"
                                  Click="TagItem_OnClick">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="80*" />
                                <ColumnDefinition Width="20*" />
                            </Grid.ColumnDefinitions>
                            <Grid.RowDefinitions>
                                <RowDefinition Height="60*" />
                                <RowDefinition Height="40*" />
                            </Grid.RowDefinitions>
                            <TextBlock Grid.Column="0" Grid.Row="0" FontSize="{Binding FontSize}" Text="{Binding Name}" />
                            <TextBlock Grid.Column="0" Grid.Row="1" FontFamily="{DynamicResource Neue Haas Grotesk Text Pro 56 Italic}" VerticalAlignment="Bottom" FontSize="12" Foreground="{StaticResource ForegroundColour75}" Text="{Binding Subname}" />
                            <TextBlock Grid.Column="1" Grid.Row="1" VerticalAlignment="Top" HorizontalAlignment="Right" TextAlignment="Right" FontSize="14" Text="{Binding Type}" />
                        </Grid>
                    </ToggleButton>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>

        <local:EntityView Visibility="Hidden" x:Name="EntityControl" Grid.Row="0" Grid.Column="1" Grid.RowSpan="2" />
        <local:StaticView Visibility="Hidden" x:Name="StaticControl" Grid.Row="0" Grid.Column="1" Grid.RowSpan="2" />
        <!-- <local:ActivityView Visibility="Hidden" x:Name="ActivityControl" Grid.Row="0" Grid.Column="1" Grid.RowSpan="2" /> -->
        <local:TextureView Visibility="Hidden" x:Name="TextureControl" Grid.Row="0" Grid.Column="1" Grid.RowSpan="2" />
        <local:CubemapView Visibility="Hidden" x:Name="CubemapControl" Grid.Row="0" Grid.Column="1" Grid.RowSpan="2" />
        <local:DialogueView Visibility="Hidden" x:Name="DialogueControl" Grid.Row="0" Grid.Column="1" Grid.RowSpan="2" />
        <local:DirectiveView Visibility="Hidden" x:Name="DirectiveControl" Grid.Row="0" Grid.Column="1" Grid.RowSpan="2" />
        <local:MusicView Visibility="Hidden" x:Name="MusicControl" Grid.Row="0" Grid.Column="1" Grid.RowSpan="2" />

        <local:ExportView x:Name="ExportControl" Visibility="Hidden" Grid.Column="1" Grid.Row="2"/>
    </Grid>
</UserControl>